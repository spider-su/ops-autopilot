# Dev overrides for investory — latest image for active development
image:
  tag: latest
  pullPolicy: Always

# Keep the development Pod within the development namespace quota.
resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
