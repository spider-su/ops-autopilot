# Legacy, unregistered compatibility artifact. Development is managed from
# clusters/dev/workloads.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: smartapp-dev
  namespace: argocd
  annotations:
    ops-autopilot.io/status: legacy-unregistered
spec:
  project: base-app
  source:
    repoURL: https://github.com/spider-su/ops-autopilot.git
    targetRevision: main
    path: applications/smartapp
    helm:
      releaseName: smartapp
      valueFiles:
        - values.yaml
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: smartapp-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
