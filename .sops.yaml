creation_rules:
  - path_regex: secrets/.*\.sops\.ya?ml$
    encrypted_regex: '^(data|stringData)$'
    # Supply the repository's age recipient with --age or SOPS_AGE_RECIPIENT.
    # Do not commit private keys or plaintext secret values.
